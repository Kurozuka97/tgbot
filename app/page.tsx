export default function Home() {
  return (
    <main style={{ fontFamily: 'monospace', padding: '2rem' }}>
      <h1>🤖 TG Bot</h1>
      <p>Webhook active at <code>/api/webhook</code></p>
    </main>
  )
}
